import React, { useEffect } from 'react';
import { GalleryWrapper } from './Gallery.style.js';

const Gallery = () => {

    useEffect(() => {}, []);

    return (<GalleryWrapper>
        <span>Gallery</span>
    </GalleryWrapper>)
};
export default Gallery;
